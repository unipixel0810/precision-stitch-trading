# Architecture Rules: Clean Architecture

이 프로젝트는 **Clean Architecture**를 따른다. 의존성은 안쪽으로만 향하며, 한 파일에 모든 계층을 섞지 않는다.

## 레이어 구조

| 레이어 | 경로 (예시) | 책임 |
|--------|-------------|------|
| **Domain** | `domain/` | 순수 데이터 모델, 도메인 규칙. **외부 라이브러리(패키지) import 금지.** 표준 라이브러리 + 프로젝트 내부 타입만 사용. |
| **Application** | `application/` | 유스케이스·앱 서비스·비즈니스 흐름. **domain만 의존**한다. |
| **Infrastructure** | `infrastructure/` | DB, REST/WebSocket API, 로컬 저장소, 키움 등 외부 연동 구현체. |
| **Presentation** | `presentation/` | UI(위젯, 화면, 라우팅), 사용자 입력·상태 표현. |

### Domain 보충

- **Repository / DataSource는 “인터페이스(추상)”만** domain에 둘 수 있다(구현 없음, 외부 패키지 없음).
- **구현 클래스**는 반드시 `infrastructure/`에 둔다.

## 의존성 방향 (필수)

```
presentation → application → domain
     ↑              ↑
infrastructure ────┴──── (application이 정의한 Port 또는 domain 인터페이스를 구현)
```

- `domain`은 **어떤 상위 레이어도 import하지 않는다.**
- `application`은 **`domain`만** import한다. (`flutter`, `dio`, `sqflite` 등 **금지**.)
- `infrastructure`는 **`domain`의 계약(인터페이스)** 과 **`application`에서 필요한 어댑터 경계**에 맞춰 의존한다. **역방향**(domain이 infrastructure를 아는 것)은 금지.
- `presentation`은 **`application`**(유스케이스, ViewModel 정책)과 UI 타입만 다룬다. **직접** `infrastructure`에 의존하지 않는 것을 원칙으로 한다(연결은 DI/주입 경계로 한 곳에서).

## 금지 사항

1. **단일 파일에 모든 로직 금지** — 화면 한 파일에 API 호출·파싱·도메인 규칙·위젯 트리를 전부 넣지 않는다.
2. **domain에 `package:flutter` / `dio` / `freezed` 등 외부 패키지 금지** (순수 Dart 유지).
3. **역방향 의존성** — 예: `domain`이 `infrastructure`를 import.

## 권장 배치 (Flutter 예시)

```
lib/
  domain/
    entities/
    repositories/     # 추상 클래스만
  application/
    use_cases/
    dtos/             # 필요 시, domain만 참조
  infrastructure/
    api/
    database/
    repositories/   # domain 계약 구현
  presentation/
    pages/
    widgets/
    providers/        # 또는 bloc/ 등
```

## 코드 리뷰 체크리스트

- [ ] 변경 파일이 위 레이어 중 어디에 속하는가?
- [ ] import 방향이 `presentation → application → domain`을 위배하지 않는가?
- [ ] domain 파일에 `package:` 외부 의존성이 없는가?
- [ ] 한 파일이 “화면 + API + 엔티티”처럼 과도하게 비대하지 않은가?

---

## Phase 작업 시 우선 순서 (필수)

**Phase 단위 작업을 진행할 때, 아래 순서를 우선 원칙으로 한다.** 앞 단계가 끝나지 않은 상태에서 뒤 단계만 진행하지 않는다.

| 순서 | 구분 | 주요 산출물 (이 프로젝트 기준) |
|------|------|----------------------------------|
| 1 | **설계 (Design)** | 요구·화면 경계 정리, `domain` 엔티티·값 객체·**리포지토리 인터페이스(Port)** 초안, `application` 유스케이스 시그니처·입출력 정리, 폴더 구조·DI 위치 합의 |
| 2 | **프론트 (Frontend)** | `presentation` UI·네비게이션·테마, 화면이 호출할 **유스케이스 조립 방식**(Provider/Bloc 등), 필요 시 **`application` 스텁·Fake Port**로 동작 가능한 수준까지 |
| 3 | **백엔드 (Backend)** | `infrastructure` 실구현(HTTP·DB·키움 등), Port 구현체·매퍼, `application`이 실 데이터와 연결되도록 조립( **`main`/composition root**에서 교체) |
| 4 | **QA** | 단위 테스트(application+fake), 위젯/통합 테스트(중요 플로), 수동 체크리스트, 회귀·접근성·다국어 등 합의된 범위 |

### 용어 매핑 (Clean Architecture와의 대응)

- **설계:** 주로 `domain` + `application`의 “계약·이름·경계” 확정 (구현 최소).
- **프론트:** 주로 `presentation` + (화면 연동을 위한) `application`의 얇은 조립; **실제 API 없이도** UI 시나리오 검증 가능하게 할 것.
- **백엔드:** 주로 `infrastructure` + 실제 저장소/API를 쓰는 저장소 구현; 필요 시 서버·브로커는 별 저장소일 수 있으나, 앱 쪽에서는 이 폴더에 **어댑터**로 모은다.
- **QA:** 기능·아키텍처 규칙(본 문서)·회귀를 검증하는 모든 자동/수동 검증.

### 예외

- 긴급 핫픽스는 최소 변경으로 **QA**만 선행할 수 있으나, 머지 전에 설계·레이어 위반 여부를 문서에 남긴다.

---

## UI·디자인 진화 원칙 (기존 화면 기준)

- **기준 레퍼런스:** 현재 Precision Dashboard 레이아웃(상단 네비 · 좌 스캐너 · 중앙 차트 · 우 0624 컨트롤러 · 하단 컨텍스트 바). 신규 작업은 이 **정보 구조·패널 비율**을 기본으로 한다.
- **토큰 단일 출처:** 루트 `design.md`(Precision Stitch)와 Flutter `presentation/theme`를 정합시킨다. 순수 장식이 아니라 **표면 계층·타이포·버튼 규칙**을 우선 적용한다.
- **변경 방식:** “새 템플릿”이 아니라 **기존 화면과 유사한 진화** — 색·간격·타이포·입력 포커스 스티치 등 점진 개선. 큰 구조 변경 시에는 스크린샷 Before/After와 **체크리스트**로 회귀 범위를 명시한다.
- **확장 영역(추가 가능):** 아래는 도메인/화면이 커질 때 붙일 수 있는 **슬롯**이다. 실제 추가 시 본 절의 Phase 표를 다시 점검하고, 필요한 **엔티티·Port·유스케이스**를 해당 Phase 설계 단계에 반영한다.
  - 예: 설정(하단 시트/모달), 스캐너 필터·정렬, 알림/토스트 스트림, 다계정·환경 스위처, 주문확인 다이얼로그, 차트 도구 프로필 저장 등.

### 디자인 작업과 “설계 → 프론트 → 백엔드 → QA” 매핑

| 단계 | UI·디자인 관점에서의 산출 |
|------|---------------------------|
| **설계** | `design.md` 대비 갭 목록, 와이어(텍스트/스케치), **신규 영역이면** 정보구조·상태 목록·domain 필드 초안 |
| **프론트** | 테마·위젯·레이아웃 반영(유사 UX 유지), 스텁 데이터로 시각 검증 |
| **백엔드** | 실데이터·설정 영속화가 UI에 필요할 때만 Port 구현 연결 |
| **QA** | 시각 회귀(스샷 또는 수동), 토큰·포커스·가독성, 접근성(최소 대비) |

---

## 프로젝트 Phase & 태스크 (P0~)

각 Phase마다 **① 설계 → ② 프론트 → ③ 백엔드 → ④ QA**를 적용한다. **UI 변경은 “기존과 유사”를 만족**해야 하며, **확장 슬롯**을 열면 해당 Phase 설계를 갱신하고 Port/유스케이스를 추가한다.

### Phase 0 — 기반·스캐폴드·기준선

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | 리팩터 **범위**(`stitch_trader`), 목표 `lib/` 트리(`domain`·`application`·`infrastructure`·`presentation`), **DI 전략** 합의 문서화. |
| 2 | 프론트 | **진입점 역할**(`main.dart`)·현재 UI 위치(`lib/screens`,`lib/theme`) vs 목표(`lib/presentation`) **이행 안내** 문서화. P0에서는 대규모 UI 이동 없음. |
| 3 | 백엔드 | **추후 연동 범위** 목록(API·키움·DB 등)만 backlog로 기록. |
| 4 | QA | `flutter analyze` **기준선** 기록, 작업 브랜치·커밋으로 스냅샷 고정. |

상세 산출물: `stitch_trader/docs/PHASE0_BASELINE.md`

### Phase 1 — Domain + 디자인 감사

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | 엔티티·값 객체·리포지토리 Port 초안(스캐너 항목, 시세 스냅, 감시 라인, 0624 설정 등). |
| 1 | 설계 | **UI 디자인 감사:** `design.md` vs 현재 Flutter 화면 갭 리스트 작성(색/간격/타이포만이라도 표로 정리). |
| 1 | 설계 | **확장 슬롯 후보** 1회 작성(위 목록 중 실제로 할 것만 체크, 나머지는 backlog). |
| 2 | 프론트 | P1에서 UI 대규모 변경은 하지 않음. 필요 시 **문서/주석**으로 “P3에서 반영할 토큰 항목”만 연결. |
| 3 | 백엔드 | 없음. |
| 4 | QA | `domain/` analyze·외부 `package:` 금지 확인. 감사표 리뷰 합의. |

### Phase 2 — Application

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | 유스케이스 시그니처·실패 케이스. **확장 기능**이 생기면 여기서 입력/출력 추가. |
| 1 | 설계 | 디자인 관점: 각 유스케이스가 노출하는 **사용자 피드백**(로딩/에러/성공)을 UX 한 줄씩 정의. |
| 2 | 프론트 | 이벤트↔유스케이스 매핑표(아직 위젯 분해 전 가능). |
| 3 | 백엔드 | 없음. |
| 4 | QA | 유스케이스 단위 테스트(Fake Port). |

### Phase 3 — Presentation 구조 + 디자인 반영

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | 페이지/위젯 파일 트리, 상태 관리 방식, **P1 감사표의 프론트 반영 우선순위** 확정. |
| 2 | 프론트 | 대시보드 분해(`pages`/`widgets`/`theme`). **기존과 유사한 시각**으로 `design.md` 갭 클로즈. **확장 슬롯**이 있으면 빈 위젯·라우트 자리 또는 플레이스홀더(“준비 중”)로 레이아웃을 유지한다. |
| 3 | 백엔드 | Fake `infrastructure` 구현 + `main` 조립(실 API 없음). |
| 4 | QA | 스모크 테스트, 수동 시각 회귀(헤더·3패널·하단 바 위치 동일). |

### Phase 4 — Infrastructure(실연동)

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | API/키움/DB 계약, DTO↔엔티티 매핑. **확장 영역 데이터 요구** 반영. |
| 2 | 프론트 | 실패·지연·빈 데이터 상태 UI(기존 톤 유지, 에러 뱃지/글래스 패널 등 `design.md`와 정합). |
| 3 | 백엔드 | 구현체·매퍼·환경별 주입. |
| 4 | QA | 통합/스테이징 수동 테스트, 네트워크 오류 시나리오. |

### Phase 5 — DI·환경·운영

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | dev/stage/prod 주입표, 모의/실거래 플래그. |
| 2 | 프론트 | 환경 표시(기존 상단/하단 스타일과 유사한 칩·텍스트). |
| 3 | 백엔드 | 시크릿·엔드포인트 연결. |
| 4 | QA | 릴리즈 체크리스트, import 방향 샘플 PR 검증. |

### Phase 6 — 문서·백로그 (지속)

| 순서 | 구분 | 태스크 |
|------|------|--------|
| 1 | 설계 | `rule.md`·`design.md`와 코드 폴더 동기화, 확장 슬롯 우선순위 재정렬. |
| 2 | 프론트 | (선택) 스토리/캡처. |
| 3 | 백엔드 | (선택) 배포 파이프라인. |
| 4 | — | 분기별 **Phase 재점검** 회의: 디자인 변경·신규 영역 시 P1~P3 설계 회고 반영. |

### 확장 영역 발생 시 공통 절차

1. **설계:** 새 UI 영역을 어느 패널/라우트에 둘지, domain 필드·Port 메서드 추가안.  
2. **프론트:** 유사 레이아웃으로 목업 또는 스텁.  
3. **백엔드:** 필요 시에만 저장소/API.  
4. **QA:** 정보 구조 변경이 있으면 **기존 3패널 플로** 회귀를 반드시 포함.
